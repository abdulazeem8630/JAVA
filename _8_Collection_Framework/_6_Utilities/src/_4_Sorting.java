public class _4_Sorting {
    static void main(String[] args) {
        /*
        Collections.sort()  Ascending natural order.
        List.sort()
        numbers.sort(null);
        numbers.sort(Comparator.reverseOrder());
         */
    }
}
