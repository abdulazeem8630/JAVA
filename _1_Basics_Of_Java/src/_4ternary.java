// ternary operator
public class _4ternary {
    public static void main(String[]args){
        int a=10, b=5,c=2,max;
        max= (a>b) ? (a>c ? a:c) : (b>c ? b:c);
        System.out.println(max);
    }
}
